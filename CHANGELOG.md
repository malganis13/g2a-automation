# 📝 Changelog

## [v1.1.1] - 2025-11-30 (🔧 Hotfix)

### 🐛 Исправления

- **Критический фикс**: Добавлен `DATABASE_FILE` в `g2a_config.py`
  - Исправлена ошибка `ImportError: cannot import name 'DATABASE_FILE'`
  - Теперь GUI запускается без ошибок

- **Fallback без Pydantic**: Добавлен fallback режим
  - Если `pydantic` не установлен, используется упрощённая конфигурация
  - Программа будет работать даже без новых зависимостей

### ✨ Новые возможности для GUI пользователей

- **START_GUI.bat / START_GUI.sh**: Запуск в 1 клик!
  - Windows: Двойной клик по `START_GUI.bat`
  - Linux/Mac: `./START_GUI.sh`
  - Автоматически создаёт venv
  - Автоматически устанавливает зависимости
  - Автоматически создаёт `.env`

- **setup_first_run.py**: Первая настройка
  - Автоматическое создание `.env`
  - Создание папок `keys/`, `result/`, `logs/`
  - Проверка зависимостей

### 📚 Документация

- **README.md**: Переписан для обычных пользователей
  - Пошаговая инструкция для не-программистов
  - Разделение на "Быстрый старт" и "Для разработчиков"
  - FAQ с решением типичных проблем

---

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС:

### 1️⃣ Подтяни изменения
```bash
cd E:\nujno\G2A_smaq
git pull origin main
```

### 2️⃣ Установи зависимости
```bash
pip install -r requirements.txt
```

### 3️⃣ Запусти GUI

**Способ 1** (проще всего):
- Двойной клик по `START_GUI.bat`

**Способ 2**:
```bash
python g2a_gui.py
```

### 4️⃣ Заполни G2A credentials

**Вариант A** (через GUI):
1. Открой программу
2. Настройки → G2A API
3. Вставь Client ID и Secret
4. Сохранить

**Вариант B** (вручную):
1. Открой `.env` файл блокнотом
2. Заполни:
```env
G2A_CLIENT_ID=твой_client_id
G2A_CLIENT_SECRET=твой_secret
```
3. Сохрани

---

## [v1.1.0] - 2025-11-30

### ✨ Новые возможности

- **Pydantic Settings**: Конфигурация через `.env` файл с валидацией
- **Loguru**: Профессиональное логирование с ротацией файлов
- **Custom Exceptions**: Типизированные исключения для лучшей обработки ошибок
- **Requirements.txt**: Зафиксированные версии зависимостей

### 🔧 Улучшения

- **g2a_config.py**: Переписан на Pydantic Settings
  - Автоматическое чтение из `.env`
  - Валидация параметров
  - Type hints
  - Backward compatibility со старым кодом

- **logging_config.py**: Новая система логирования
  - Цветной вывод в консоль
  - Автоматическая ротация лог-файлов
  - Раздельные файлы для errors
  - Backtrace для дебага

- **exceptions.py**: Иерархия кастомных исключений
  - `G2AAPIError` - ошибки API
  - `AuthenticationError` - 401 ошибки
  - `RateLimitError` - 429 ошибки
  - `DatabaseError` - ошибки БД
  - `PriceParsingError` - ошибки парсинга

- **.gitignore**: Обновлён
  - Игнорирование `.env`
  - Игнорирование `*.db`
  - Игнорирование `logs/`

### 📚 Документация

- Добавлен `INSTALL.md` с подробной инструкцией
- `.env.example` - шаблон конфигурации

### ⚠️ Breaking Changes

Нет! Все изменения обратно совместимы.

---

## 📝 TODO (следующие улучшения)

- [ ] Добавить retry в `g2a_api_client.py` через `tenacity`
- [ ] Переписать `database.py` с лучшим логированием
- [ ] Обновить `main.py` с новым конфигом
- [ ] Добавить юнит-тесты
- [ ] Docker контейнеризация
