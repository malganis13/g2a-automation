# 🚀 Установка G2A Automation

## Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/malganis13/g2a-automation.git
cd g2a-automation
```

## Шаг 2: Создание виртуального окружения

### Windows
```bash
python -m venv venv
venv\Scripts\activate
```

### Linux/Mac
```bash
python3 -m venv venv
source venv/bin/activate
```

## Шаг 3: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

## Шаг 4: Настройка конфигурации

### 1. Скопируй `.env.example` в `.env`:

**Windows:**
```bash
copy .env.example .env
```

**Linux/Mac:**
```bash
cp .env.example .env
```

### 2. Открой `.env` и заполни свои данные:

```env
G2A_CLIENT_ID=твой_client_id
G2A_CLIENT_SECRET=твой_secret
```

### 🔑 Где взять G2A Credentials?

1. Зайди в [G2A Dashboard](https://dashboard.g2a.com/)
2. **Settings** → **API Integration**
3. Скопируй **Client ID** и **Client Secret**
4. Вставь в `.env` файл

## Шаг 5: Запуск

```bash
python main.py
```

## 🛠️ Троублшутинг

### ❌ Ошибка "G2A credentials not configured"

→ Проверь что:
- `.env` файл существует в корне проекта
- В `.env` заполнены `G2A_CLIENT_ID` и `G2A_CLIENT_SECRET`
- Нет лишних пробелов в значениях

### ❌ Ошибка импорта модулей

→ Убедись что виртуальное окружение активировано:

```bash
# Должно быть (venv) в начале строки
(venv) C:\path\to\project>
```

Если нет - активируй:
- Windows: `venv\Scripts\activate`
- Linux/Mac: `source venv/bin/activate`

### ❌ База данных не создаётся

→ Проверь права на запись в папке проекта

### ❌ Ошибка 401 Unauthorized

→ Проверь что G2A credentials правильные:
1. Зайди в G2A Dashboard
2. Перегенерируй API credentials
3. Обнови в `.env`

## 📝 Примечания

- **НЕ коммить** `.env` файл в git!
- **НЕ коммить** `keys.db` базу данных!
- Логи сохраняются в папку `logs/`
- Результаты парсинга в `result/`

## ⚙️ Дополнительная настройка

### Telegram уведомления (опционально)

Добавь в `.env`:
```env
TELEGRAM_BOT_TOKEN=твой_токен
TELEGRAM_CHAT_ID=твой_chat_id
```

### Proxy (опционально)

```env
PROXY_URL=http://user:pass@host:port
```

### Уровень логирования

```env
LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR
```

## 🚀 Что дальше?

1. Запусти `python main.py`
2. Выбери режим работы
3. Добавь ключи через меню
4. Запусти парсинг цен
5. Создай офферы на G2A

Удачи! 🎉
